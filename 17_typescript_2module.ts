import {Geometry} from "./17_typescript_1module";

const radious: number = 5;

console.log(`Circumference: ${Geometry.calculateCircumference(radious)}`)
console.log(`Area: ${Geometry.calculateArea(radious)}`)
